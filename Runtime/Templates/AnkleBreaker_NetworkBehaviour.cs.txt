using System;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;
using AnkleBreaker.Core.MasterClasses;

/// <summary>
/// Network behaviour template. Requires a networking framework (e.g. FishNet).
/// Replace AnkleBreakerMonoBehaviour with your network behaviour base class.
/// </summary>
public class #SCRIPTNAME# : AnkleBreakerMonoBehaviour
{
    #region Serialized Fields
    #NOTRIM#
    #endregion

    #region Private Fields
    #NOTRIM#
    #endregion

    protected override void EventHandlerRegister()
    {
        //if (IsOwner)
        #NOTRIM#
        //if (IsClient)
        #NOTRIM#
        //if (IsServer)
        #NOTRIM#
    }

    protected override void EventHandlerUnRegister()
    {
        // NEVER conditional — one -= for every possible +=
        #NOTRIM#
    }

    public override void Start()
    {
        base.Start();
    }
}

public static class #SCRIPTNAME#HandlerData
{
    #region ServerEvents
    public static event Action OnServerEventRequest;
    public static void ServerEventRequest() => OnServerEventRequest?.Invoke();
    #endregion

    #region ClientEvents
    public static event Action OnClientEventRequest;
    public static void ClientEventRequest() => OnClientEventRequest?.Invoke();
    #endregion
}
